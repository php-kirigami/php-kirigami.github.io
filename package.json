{
  "name": "php-kirigami.github.io",
  "version": "0.1.0",
  "description": "The Kirigami project site — built with Kirigami.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "serve": "kiri serve",
    "build": "kiri build",
    "watch": "kiri watch",
    "export": "kiri export"
  },
  "devDependencies": {
    "@kirigami/cli": "^0.1.1",
    "@kirigami/kirigami": "^3.0.1",
    "@kirigami/canva": "^2.7.0",
    "@kirigami/plugin-embed": "^0.2.0",
    "@kirigami/plugin-extlink": "^0.2.0",
    "@kirigami/plugin-highlight": "^0.2.0"
  }
}
