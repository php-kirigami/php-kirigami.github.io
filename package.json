{
  "name": "php-kirigami.github.io",
  "version": "0.1.0",
  "description": "The Kirigami project site — built with Kirigami.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "build": "kiri build",
    "watch": "kiri watch",
    "export": "kiri export"
  },
  "devDependencies": {
    "@kirigami/kirigami": "^2.0.0",
    "@kirigami/canva": "^2.6.0",
    "@kirigami/plugin-embed": "^0.1.5",
    "@kirigami/plugin-extlink": "^0.1.3",
    "@kirigami/plugin-highlight": "^0.1.7"
  }
}
