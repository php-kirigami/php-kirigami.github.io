{
  "name": "php-kirigami.github.io",
  "version": "0.1.0",
  "description": "The Kirigami project site — built with Kirigami.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "build": "kiri build",
    "watch": "kiri watch",
    "export": "kiri export"
  },
  "devDependencies": {
    "@kirigami/kirigami": "^1.4.0",
    "@kirigami/canva": "^2.4.0",
    "@kirigami/plugin-highlight": "^0.1.1"
  }
}
